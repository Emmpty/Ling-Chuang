import { FuncButtonTrigger } from "../enums";
import Button from "../abstract/button";
import Painter from "../painter";
import Rect from "../rect";
import Vector from "../vector";
import ViewObject from "../abstract/view-object";
declare class RotateButton extends Button {
    trigger: FuncButtonTrigger;
    rect: Rect;
    master: ViewObject;
    private oldViewObjectRect;
    private oldRadius;
    oldAngle: number;
    radius: number;
    private disable;
    relativeRect: Rect;
    key: string | number;
    constructor(master: ViewObject);
    updatePosition(vector: Vector): void;
    setMaster(master: ViewObject): void;
    /**
     * 为拖拽改变大小初始化
     */
    private initScale;
    effect(newRect: Rect): void;
    get getOldAngle(): number;
    update(paint: Painter): void;
    onSelected(): void;
    hide(): void;
    show(): void;
    draw(paint: Painter): void;
    drawButton(position: Vector, size: Size, radius: number, paint: Painter): void;
}
export default RotateButton;
//# sourceMappingURL=rotateButton.d.ts.map