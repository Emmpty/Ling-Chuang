import Painter from "../../painter";
import Vector from "../../vector";
import ViewObject from "../view-object";
export interface ViewLines {
    view: ViewObject;
    points: Array<Vector>;
}
/**
 * 辅助线显示
 */
declare class AuxiliaryLine {
    private readonly d;
    private readonly dash;
    private readonly color;
    private referencePoint;
    createReferencePoint(key: string): void;
    /**
     * 遍历五个点
     */
    private getPoint;
    draw(paint: Painter, view: ViewObject): void;
}
export default AuxiliaryLine;
//# sourceMappingURL=auxiliary-lines.d.ts.map