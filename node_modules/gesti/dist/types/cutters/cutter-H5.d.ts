import CutterInterface from "../interfaces/cutter";
import Painter from "../painter";
import { ImageChunk } from "../types/index";
import XImage from "../ximage";
/**
 * 图片切割
 * 只做图片切割工作，其他不管
 */
declare class CutterH5 implements CutterInterface {
    painter: Painter;
    private offScreenPainter;
    /**
     * @description offScreen为false时，你必须要传入一个painter对象
     * @param offScreen
     * @param painter
     */
    constructor(offScreen?: boolean, painter?: Painter);
    /**
     * @description 切割图片成小块
     * @param chunkSize
     * @param ximage
     * @param offset
     * @returns
     */
    getChunks(chunkSize: number, ximage: XImage): Promise<ImageChunk[]>;
    merge(width: number, height: number, chunks: ImageChunk[]): Promise<ImageData>;
}
export default CutterH5;
//# sourceMappingURL=cutter-H5.d.ts.map