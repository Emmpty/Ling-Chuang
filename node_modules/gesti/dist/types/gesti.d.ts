import GestiConfig, { gesticonfig } from "./config/gestiConfig";
import { ViewObjectFamily } from "./enums";
import GestiController from "./interfaces/gesticontroller";
import XImage from "./ximage";
/**
 * 初始化该 @Gesti 实例时，由于平台不确定，用户必须传入 @CanvasRenderingContext2D 画笔作为
 */
declare class Gesti {
    private kit;
    static XImage: typeof XImage;
    static config: GestiConfig;
    constructor(config?: gesticonfig);
    private _controller;
    get controller(): GestiController;
    set debug(value: boolean);
    init(canvas: HTMLCanvasElement | null, paint?: CanvasRenderingContext2D | null, rect?: {
        x?: number;
        y?: number;
        width: number;
        height: number;
    }): void;
    /**
     * @description 设置配置，跟构造函数一样
     * @param config
     */
    setConfig(config?: gesticonfig): void;
    destroy(): void;
    static Family: typeof ViewObjectFamily;
}
export default Gesti;
//# sourceMappingURL=gesti.d.ts.map